

<table class="gridtable">
  <tr>
    <th>Date</th>
    <th>Bank account</th>
    <th>Description</th>
    <th>Amount</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @bank_penalties.each do |bank_penalty| %>
  <tr>
    <td><%= bank_penalty.date %></td>
    <td><%= bank_penalty.bank_account %></td>
    <td><%= bank_penalty.description %></td>
    <td><%= bank_penalty.amount %></td>
    <td><%= link_to 'Show', [@account, @accounting_period, bank_penalty], :data => { :colorbox => true} %></td>
    <td><%= link_to 'Edit', edit_account_accounting_period_bank_penalty_path(@account, @accounting_period, bank_penalty), :data => { :colorbox => true} %></td>
    <td><%= link_to 'Destroy', [@account, @accounting_period, bank_penalty], method: :delete, data: { confirm: 'Are you sure?' }, remote: true %></td>
  </tr>
<% end %>
</table>
