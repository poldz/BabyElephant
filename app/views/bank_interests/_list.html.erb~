<table class="gridtable">
  <tr>
    <th>Date</th>
    <th>Bank account</th>
    <th>Amount</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @bank_interests.each do |bank_interest| %>
  <tr>
    <td><%= bank_interest.date %></td>
    <td><%= bank_interest.bank_account %></td>
    <td><%= bank_interest.amount %></td>
    <td><%= link_to 'Show', [@account, @accounting_period, bank_interest], :data => { :colorbox => true} %></td>
    <td><%= link_to 'Edit', edit_account_accounting_period_bank_interest_path(@account, @accounting_period, bank_interest), :data => { :colorbox => true} %></td>
    <td><%= link_to 'Destroy', [@account, @accounting_period, bank_interest], method: :delete, data: { confirm: 'Are you sure?' }, remote: true %></td>
  </tr>
<% end %>
</table>

